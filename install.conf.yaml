- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
#    ~/.vimrc: vimrc

- create:
   -  ~/Work

- shell:
   -
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
