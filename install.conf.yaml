- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
#    ~/.vimrc: vimrc

- create:
   -  ~/Work

# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
