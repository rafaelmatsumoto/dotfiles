- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
#    ~/.vimrc: vimrc

- create:
   -  ~/Work


# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
