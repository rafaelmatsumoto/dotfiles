- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.zshenv: zshenv
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.ssh/config: config/ssh/config
    ~/.zplug:
      create: true
      path: zplug
    ~/.tmux:
      create: true
      path: tmux
    ~/.config/bat:
      create: true
      path: config/bat
    ~/.config/nvim:
      create: true
      path: config/nvim
    ~/.config/iterm2-config:
      create: true
      path: config/iterm2-config

- create:
   -  ~/Work

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
      description: "Install Homebrew and packages"
    - command: ./setup_zsh.zsh
      stdout: true
      stderr: true
      description: "Configure Zsh as default shell"
    - command: ./scripts/setup_tmux_plugins.zsh
      stdout: true
      stderr: true
      description: "Install Tmux plugins"
      if: '[ -f ~/.tmux.conf ]'
    - command: nvim --headless "+Lazy! sync" +qa
      description: "Syncing Neovim plugins and compiling LuaSnip"
